# Harmonic Nuclear Model - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0  # For parquet file reading

# Optional dependencies (recommended for visualization)
matplotlib>=3.7.0

# Optional dependencies (for extended analysis)
scipy>=1.10.0    # If using statistical tests
seaborn>=0.12.0  # If generating publication figures
